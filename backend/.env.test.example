# Server Configuration
NODE_ENV=test
PORT=5001

# Database (Test DB)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/clothing_test?retryWrites=true&w=majority

# JWT secret for tests only
JWT_SECRET=test_jwt_secret_here

# Email (Optional - not required for tests)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_app_password
EMAIL_FROM=your_email@gmail.com
